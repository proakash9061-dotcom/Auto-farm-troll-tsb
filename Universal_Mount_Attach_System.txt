--[[
    Universal Mount & Attach System
    Roblox Studio / Private Experience Edition
    ------------------------------------------------------------
    Purpose:
      Educational character-positioning utility for experiences
      you own or are authorized to test.

    Features:
      * Vertical Rayfield-style UI (self-contained fallback UI)
      * Player + NPC/Dummy target discovery
      * DisplayName + Username formatting
      * Mount distance: 0.5 -> 5 studs
      * Height offset: -3 -> +3 studs
      * RunService.PreSimulation positioning
      * Model:PivotTo() attachment
      * Collision / touch / query protection
      * Linear + angular velocity protection
      * Dynamic target refresh
      * Connection cleanup
      * Respawn-safe character handling

    IMPORTANT:
      This source is intended for Roblox Studio/private experiences.
      If you use Rayfield, replace the UI adapter section with your
      authorized Rayfield build if desired. This file intentionally
      contains a self-contained GUI so it can be tested without a
      third-party loader.
]]

--// Services
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")

local LocalPlayer = Players.LocalPlayer

--// Configuration
local CONFIG = {
    MountDistance = 2,
    HeightOffset = 0,

    -- Set true to make the local character non-collidable while mounted.
    NoCollisionWhileMounted = true,

    -- Defensive physics handling for the local character.
    ZeroAssemblyVelocity = true,

    -- Only accept models containing a Humanoid and a root part.
    RequireHumanoid = true,
}

--==============================================================
-- Utility
--==============================================================

local function getCharacterRoot(model)
    if not model or not model:IsA("Model") then
        return nil
    end

    return model:FindFirstChild("HumanoidRootPart")
        or model.PrimaryPart
end

local function getHumanoid(model)
    if not model or not model:IsA("Model") then
        return nil
    end

    return model:FindFirstChildOfClass("Humanoid")
end

local function isValidTarget(model)
    if not model or not model:IsA("Model") then
        return false
    end

    if model == LocalPlayer.Character then
        return false
    end

    local root = getCharacterRoot(model)

    if not root then
        return false
    end

    if CONFIG.RequireHumanoid and not getHumanoid(model) then
        return false
    end

    return true
end

local function formatPlayerTarget(player)
    return string.format(
        "PLAYER  |  %s  (@%s)",
        player.DisplayName,
        player.Name
    )
end

local function formatNpcTarget(model)
    return string.format("NPC  |  %s", model.Name)
end

--==============================================================
-- Target Manager
--==============================================================

local TargetManager = {}
TargetManager.Targets = {}
TargetManager.Selected = nil

function TargetManager:Clear()
    table.clear(self.Targets)
end

function TargetManager:Add(label, model)
    if isValidTarget(model) then
        self.Targets[label] = model
    end
end

function TargetManager:Refresh()
    self:Clear()

    -- Players
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character then
            if isValidTarget(player.Character) then
                self:Add(formatPlayerTarget(player), player.Character)
            end
        end
    end

    -- Workspace NPCs / Dummies
    for _, instance in ipairs(Workspace:GetDescendants()) do
        if instance:IsA("Model")
            and instance ~= LocalPlayer.Character
            and not Players:GetPlayerFromCharacter(instance)
            and isValidTarget(instance) then

            self:Add(formatNpcTarget(instance), instance)
        end
    end

    return self:GetLabels()
end

function TargetManager:GetLabels()
    local labels = {}

    for label in pairs(self.Targets) do
        table.insert(labels, label)
    end

    table.sort(labels)

    return labels
end

function TargetManager:SetSelected(label)
    local target = self.Targets[label]

    if target and isValidTarget(target) then
        self.Selected = target
        return true
    end

    self.Selected = nil
    return false
end

function TargetManager:GetSelected()
    if self.Selected and isValidTarget(self.Selected) then
        return self.Selected
    end

    self.Selected = nil
    return nil
end

--==============================================================
-- Character Protection
--==============================================================

local DefenseController = {}

DefenseController.Saved = {}
DefenseController.Active = false

function DefenseController:SavePart(part)
    if self.Saved[part] then
        return
    end

    self.Saved[part] = {
        CanCollide = part.CanCollide,
        CanTouch = part.CanTouch,
        CanQuery = part.CanQuery,
    }
end

function DefenseController:Enable()
    local character = LocalPlayer.Character

    if not character then
        return
    end

    self.Active = true

    for _, instance in ipairs(character:GetDescendants()) do
        if instance:IsA("BasePart") then
            self:SavePart(instance)

            if CONFIG.NoCollisionWhileMounted then
                instance.CanCollide = false
            end

            instance.CanTouch = false
            instance.CanQuery = false
        end
    end
end

function DefenseController:Apply()
    if not self.Active then
        return
    end

    local character = LocalPlayer.Character

    if not character then
        return
    end

    for _, instance in ipairs(character:GetDescendants()) do
        if instance:IsA("BasePart") then
            self:SavePart(instance)

            if CONFIG.NoCollisionWhileMounted then
                instance.CanCollide = false
            end

            instance.CanTouch = false
            instance.CanQuery = false
        end
    end
end

function DefenseController:Disable()
    self.Active = false

    for part, properties in pairs(self.Saved) do
        if part and part.Parent then
            part.CanCollide = properties.CanCollide
            part.CanTouch = properties.CanTouch
            part.CanQuery = properties.CanQuery
        end
    end

    table.clear(self.Saved)
end

function DefenseController:ZeroVelocity()
    if not self.Active or not CONFIG.ZeroAssemblyVelocity then
        return
    end

    local character = LocalPlayer.Character
    if not character then
        return
    end

    local root = getCharacterRoot(character)
    if not root then
        return
    end

    root.AssemblyLinearVelocity = Vector3.zero
    root.AssemblyAngularVelocity = Vector3.zero
end

--==============================================================
-- Mount Controller
--==============================================================

local MountController = {}

MountController.Enabled = false
MountController.Connection = nil
MountController.CharacterConnection = nil

function MountController:CalculateCFrame(target)
    local targetRoot = getCharacterRoot(target)

    if not targetRoot then
        return nil
    end

    -- Behind the target:
    -- target position - target LookVector * distance
    local behindPosition =
        targetRoot.Position
        - (targetRoot.CFrame.LookVector * CONFIG.MountDistance)

    local finalPosition =
        behindPosition
        + Vector3.new(0, CONFIG.HeightOffset, 0)

    -- Face the same direction as the target.
    return CFrame.lookAt(
        finalPosition,
        finalPosition + targetRoot.CFrame.LookVector
    )
end

function MountController:Step()
    if not self.Enabled then
        return
    end

    local character = LocalPlayer.Character
    local target = TargetManager:GetSelected()

    if not character or not target then
        return
    end

    local humanoid = getHumanoid(character)
    local root = getCharacterRoot(character)

    if not humanoid or not root then
        return
    end

    local targetRoot = getCharacterRoot(target)

    if not targetRoot then
        self:Disable()
        return
    end

    local mountCFrame = self:CalculateCFrame(target)

    if not mountCFrame then
        return
    end

    -- PivotTo is used instead of repeatedly setting individual parts.
    character:PivotTo(mountCFrame)

    DefenseController:Apply()
    DefenseController:ZeroVelocity()
end

function MountController:Enable()
    if self.Enabled then
        return
    end

    local target = TargetManager:GetSelected()

    if not target then
        return false, "Select a valid target first."
    end

    self.Enabled = true

    DefenseController:Enable()

    self.Connection = RunService.PreSimulation:Connect(function()
        self:Step()
    end)

    return true
end

function MountController:Disable()
    self.Enabled = false

    if self.Connection then
        self.Connection:Disconnect()
        self.Connection = nil
    end

    DefenseController:Disable()
end

function MountController:Toggle()
    if self.Enabled then
        self:Disable()
        return false
    end

    local success = self:Enable()

    return success == true
end

function MountController:Destroy()
    self:Disable()

    if self.CharacterConnection then
        self.CharacterConnection:Disconnect()
        self.CharacterConnection = nil
    end
end

MountController.CharacterConnection =
    LocalPlayer.CharacterAdded:Connect(function()
        task.wait(0.5)

        if MountController.Enabled then
            DefenseController:Enable()
        end
    end)

--==============================================================
-- Self-Contained Rayfield-Style Vertical GUI
--==============================================================

local GuiController = {}

local gui = Instance.new("ScreenGui")
gui.Name = "UniversalMountGUI"
gui.ResetOnSpawn = false
gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
gui.Parent = LocalPlayer:WaitForChild("PlayerGui")

local main = Instance.new("Frame")
main.Name = "Main"
main.Size = UDim2.fromOffset(330, 510)
main.Position = UDim2.new(0, 25, 0.5, -255)
main.BackgroundColor3 = Color3.fromRGB(18, 18, 24)
main.BorderSizePixel = 0
main.Parent = gui

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 12)
corner.Parent = main

local stroke = Instance.new("UIStroke")
stroke.Color = Color3.fromRGB(70, 70, 90)
stroke.Thickness = 1
stroke.Parent = main

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, -25, 0, 42)
title.Position = UDim2.fromOffset(15, 8)
title.BackgroundTransparency = 1
title.Text = "UNIVERSAL MOUNT"
title.TextColor3 = Color3.fromRGB(235, 235, 245)
title.Font = Enum.Font.GothamBold
title.TextSize = 18
title.TextXAlignment = Enum.TextXAlignment.Left
title.Parent = main

local subtitle = Instance.new("TextLabel")
subtitle.Size = UDim2.new(1, -25, 0, 20)
subtitle.Position = UDim2.fromOffset(15, 43)
subtitle.BackgroundTransparency = 1
subtitle.Text = "Studio / Private Experience Utility"
subtitle.TextColor3 = Color3.fromRGB(145, 145, 165)
subtitle.Font = Enum.Font.Gotham
subtitle.TextSize = 11
subtitle.TextXAlignment = Enum.TextXAlignment.Left
subtitle.Parent = main

local scroll = Instance.new("ScrollingFrame")
scroll.Size = UDim2.new(1, -20, 1, -80)
scroll.Position = UDim2.fromOffset(10, 72)
scroll.BackgroundTransparency = 1
scroll.BorderSizePixel = 0
scroll.ScrollBarThickness = 4
scroll.CanvasSize = UDim2.fromOffset(0, 0)
scroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
scroll.Parent = main

local layout = Instance.new("UIListLayout")
layout.Padding = UDim.new(0, 8)
layout.SortOrder = Enum.SortOrder.LayoutOrder
layout.Parent = scroll

local padding = Instance.new("UIPadding")
padding.PaddingBottom = UDim.new(0, 12)
padding.Parent = scroll

local function makeLabel(text, height)
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, -8, 0, height or 28)
    label.BackgroundTransparency = 1
    label.Text = text
    label.TextColor3 = Color3.fromRGB(180, 180, 195)
    label.Font = Enum.Font.GothamMedium
    label.TextSize = 12
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = scroll
    return label
end

local function makeButton(text, height)
    local button = Instance.new("TextButton")
    button.Size = UDim2.new(1, -8, 0, height or 38)
    button.BackgroundColor3 = Color3.fromRGB(32, 32, 42)
    button.Text = text
    button.TextColor3 = Color3.fromRGB(235, 235, 245)
    button.Font = Enum.Font.GothamMedium
    button.TextSize = 12
    button.AutoButtonColor = true
    button.Parent = scroll

    local c = Instance.new("UICorner")
    c.CornerRadius = UDim.new(0, 8)
    c.Parent = button

    return button
end

local function makeSlider(titleText, minValue, maxValue, initialValue, callback)
    local container = Instance.new("Frame")
    container.Size = UDim2.new(1, -8, 0, 60)
    container.BackgroundColor3 = Color3.fromRGB(25, 25, 33)
    container.Parent = scroll

    local c = Instance.new("UICorner")
    c.CornerRadius = UDim.new(0, 8)
    c.Parent = container

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, -20, 0, 25)
    label.Position = UDim2.fromOffset(10, 4)
    label.BackgroundTransparency = 1
    label.TextColor3 = Color3.fromRGB(215, 215, 225)
    label.Font = Enum.Font.GothamMedium
    label.TextSize = 12
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = container

    local bar = Instance.new("TextButton")
    bar.Size = UDim2.new(1, -20, 0, 8)
    bar.Position = UDim2.fromOffset(10, 38)
    bar.BackgroundColor3 = Color3.fromRGB(55, 55, 68)
    bar.Text = ""
    bar.AutoButtonColor = false
    bar.Parent = container

    local bc = Instance.new("UICorner")
    bc.CornerRadius = UDim.new(1, 0)
    bc.Parent = bar

    local fill = Instance.new("Frame")
    fill.BackgroundColor3 = Color3.fromRGB(110, 110, 255)
    fill.BorderSizePixel = 0
    fill.Parent = bar

    local fc = Instance.new("UICorner")
    fc.CornerRadius = UDim.new(1, 0)
    fc.Parent = fill

    local dragging = false
    local value = initialValue

    local function setValue(v)
        value = math.clamp(v, minValue, maxValue)

        local alpha = (value - minValue) / (maxValue - minValue)
        fill.Size = UDim2.new(alpha, 0, 1, 0)

        label.Text = string.format("%s: %.1f", titleText, value)

        callback(value)
    end

    local function fromX(x)
        local alpha =
            math.clamp(
                (x - bar.AbsolutePosition.X) / bar.AbsoluteSize.X,
                0,
                1
            )

        setValue(minValue + ((maxValue - minValue) * alpha))
    end

    bar.MouseButton1Down:Connect(function(x)
        dragging = true
        fromX(x)
    end)

    bar.MouseButton1Up:Connect(function()
        dragging = false
    end)

    bar.MouseMoved:Connect(function(x)
        if dragging then
            fromX(x)
        end
    end)

    setValue(initialValue)

    return {
        SetValue = setValue,
        GetValue = function()
            return value
        end,
    }
end

-- Target section
makeLabel("TARGET", 24)

local targetButton = makeButton("No target selected", 42)
local refreshButton = makeButton("↻  Refresh Targets", 36)

local targetLabels = {}

local function refreshTargets()
    targetLabels = TargetManager:Refresh()

    if #targetLabels == 0 then
        targetButton.Text = "No player/NPC targets found"
        return
    end

    targetButton.Text = "Select Target  •  " .. tostring(#targetLabels)
end

-- Simple target picker window
local picker = Instance.new("Frame")
picker.Size = UDim2.fromOffset(300, 300)
picker.Position = UDim2.new(0, 35, 0.5, -150)
picker.BackgroundColor3 = Color3.fromRGB(20, 20, 27)
picker.Visible = false
picker.ZIndex = 20
picker.Parent = gui

local pc = Instance.new("UICorner")
pc.CornerRadius = UDim.new(0, 10)
pc.Parent = picker

local pickerTitle = Instance.new("TextLabel")
pickerTitle.Size = UDim2.new(1, -20, 0, 35)
pickerTitle.Position = UDim2.fromOffset(10, 5)
pickerTitle.BackgroundTransparency = 1
pickerTitle.Text = "Select Target"
pickerTitle.TextColor3 = Color3.fromRGB(235, 235, 245)
pickerTitle.Font = Enum.Font.GothamBold
pickerTitle.TextSize = 14
pickerTitle.TextXAlignment = Enum.TextXAlignment.Left
pickerTitle.ZIndex = 21
pickerTitle.Parent = picker

local pickerScroll = Instance.new("ScrollingFrame")
pickerScroll.Size = UDim2.new(1, -20, 1, -50)
pickerScroll.Position = UDim2.fromOffset(10, 42)
pickerScroll.BackgroundTransparency = 1
pickerScroll.BorderSizePixel = 0
pickerScroll.ScrollBarThickness = 4
pickerScroll.ZIndex = 21
pickerScroll.Parent = picker

local pickerLayout = Instance.new("UIListLayout")
pickerLayout.Padding = UDim.new(0, 5)
pickerLayout.Parent = pickerScroll

local function rebuildPicker()
    for _, child in ipairs(pickerScroll:GetChildren()) do
        if child:IsA("TextButton") then
            child:Destroy()
        end
    end

    for _, label in ipairs(targetLabels) do
        local button = Instance.new("TextButton")
        button.Size = UDim2.new(1, -5, 0, 35)
        button.BackgroundColor3 = Color3.fromRGB(32, 32, 42)
        button.Text = label
        button.TextColor3 = Color3.fromRGB(225, 225, 235)
        button.Font = Enum.Font.Gotham
        button.TextSize = 11
        button.TextTruncate = Enum.TextTruncate.AtEnd
        button.ZIndex = 22
        button.Parent = pickerScroll

        local c = Instance.new("UICorner")
        c.CornerRadius = UDim.new(0, 6)
        c.Parent = button

        button.MouseButton1Click:Connect(function()
            if TargetManager:SetSelected(label) then
                targetButton.Text = label
            end

            picker.Visible = false
        end)
    end

    pickerScroll.CanvasSize = UDim2.fromOffset(
        0,
        math.max(0, #targetLabels * 40)
    )
end

targetButton.MouseButton1Click:Connect(function()
    targetLabels = TargetManager:Refresh()
    rebuildPicker()
    picker.Visible = true
end)

refreshButton.MouseButton1Click:Connect(function()
    refreshTargets()
    rebuildPicker()
end)

-- Mount section
makeLabel("MOUNT", 24)

local distanceSlider = makeSlider(
    "Distance",
    0.5,
    5,
    CONFIG.MountDistance,
    function(value)
        CONFIG.MountDistance = math.round(value * 10) / 10
    end
)

local heightSlider = makeSlider(
    "Height",
    -3,
    3,
    CONFIG.HeightOffset,
    function(value)
        CONFIG.HeightOffset = math.round(value * 10) / 10
    end
)

local mountButton = makeButton("●  Mount: OFF", 42)

mountButton.MouseButton1Click:Connect(function()
    if MountController.Enabled then
        MountController:Disable()
        mountButton.Text = "●  Mount: OFF"
        return
    end

    local success, message = MountController:Enable()

    if success then
        mountButton.Text = "●  Mount: ON"
    else
        targetButton.Text = message or "Select a target first"
    end
end)

-- Defense section
makeLabel("DEFENSE / PHYSICS", 24)

local defenseButton = makeButton(
    "Protection: " .. (CONFIG.NoCollisionWhileMounted and "ON" or "OFF"),
    36
)

defenseButton.MouseButton1Click:Connect(function()
    CONFIG.NoCollisionWhileMounted = not CONFIG.NoCollisionWhileMounted

    defenseButton.Text =
        "Protection: "
        .. (CONFIG.NoCollisionWhileMounted and "ON" or "OFF")

    if MountController.Enabled then
        if CONFIG.NoCollisionWhileMounted then
            DefenseController:Enable()
        end
    end
end)

local velocityButton = makeButton(
    "Velocity Protection: "
        .. (CONFIG.ZeroAssemblyVelocity and "ON" or "OFF"),
    36
)

velocityButton.MouseButton1Click:Connect(function()
    CONFIG.ZeroAssemblyVelocity = not CONFIG.ZeroAssemblyVelocity

    velocityButton.Text =
        "Velocity Protection: "
        .. (CONFIG.ZeroAssemblyVelocity and "ON" or "OFF")
end)

-- Status
makeLabel("STATUS", 24)

local status = makeLabel("Ready • No target selected", 36)
status.TextColor3 = Color3.fromRGB(145, 190, 170)

-- Update status periodically without creating an attachment loop.
task.spawn(function()
    while gui.Parent do
        task.wait(0.25)

        local target = TargetManager:GetSelected()

        if MountController.Enabled and target then
            status.Text =
                "Mounted • "
                .. target.Name
                .. string.format(
                    " • %.1f studs",
                    CONFIG.MountDistance
                )
        elseif target then
            status.Text = "Ready • Target: " .. target.Name
        else
            status.Text = "Ready • No target selected"
        end
    end
end)

--==============================================================
-- Player / Workspace refresh events
--==============================================================

Players.PlayerAdded:Connect(function()
    task.defer(function()
        refreshTargets()
    end)
end)

Players.PlayerRemoving:Connect(function(player)
    local selected = TargetManager:GetSelected()

    if selected and Players:GetPlayerFromCharacter(selected) == player then
        MountController:Disable()
        mountButton.Text = "●  Mount: OFF"
    end

    task.defer(function()
        refreshTargets()
    end)
end)

Workspace.DescendantAdded:Connect(function(instance)
    if instance:IsA("Model") then
        task.defer(function()
            refreshTargets()
        end)
    end
end)

Workspace.DescendantRemoving:Connect(function(instance)
    if instance:IsA("Model") then
        if TargetManager.Selected == instance then
            MountController:Disable()
            mountButton.Text = "●  Mount: OFF"
        end

        task.defer(function()
            refreshTargets()
        end)
    end
end)

--==============================================================
-- Initial state
--==============================================================

refreshTargets()

-- Expose controllers for Studio development/debugging.
_G.UniversalMount = {
    Config = CONFIG,
    Targets = TargetManager,
    Mount = MountController,
    Defense = DefenseController,
    GUI = GuiController,
}

print("[UniversalMount] Loaded successfully.")
print("[UniversalMount] Studio/private-experience utility ready.")
