--[[
    Lunar Combat Suite v3 — Stability Patch + Auto-Rotate FOV
    Silent aim reentrancy fixed. Head-only lock. Auto-rotate 360° targeting.
    Minimize/expand UI. Mobile-hardened.
--]]

-- ============ SERVICES ============
local Players          = game:GetService("Players")
local RunService       = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Workspace        = game:GetService("Workspace")
local CoreGui          = game:GetService("CoreGui")

local LocalPlayer = Players.LocalPlayer
local Camera      = Workspace.CurrentCamera
local Mouse       = LocalPlayer:GetMouse()

-- ============ ANTI-CHEAT HARDENING ============
pcall(function()
    if setthreadidentity then setthreadidentity(8) end
    if syn and syn.set_thread_identity then syn.set_thread_identity(8) end
end)

pcall(function()
    getcallingscript = function() return LocalPlayer.Character or game end
    getcallingscriptname = function() return "LocalScript" end
end)

-- ============ SECURE PARENT ============
local function secureParent(instance)
    local ok = pcall(function() instance.Parent = CoreGui end)
    if not ok then
        local pg = LocalPlayer:FindFirstChildOfClass("PlayerGui")
            or LocalPlayer:WaitForChild("PlayerGui")
        local hidden = pg:FindFirstChild("RobloxChat") or Instance.new("Folder", pg)
        hidden.Name = "RobloxChat"
        instance.Parent = hidden
    end
end

-- ============ CONFIG ============
local Config = {
    Aimbot = {
        Enabled = false,
        Key = Enum.UserInputType.MouseButton2,
        TeamCheck = true,
        WallCheck = false,
        FOV = 120,
        Smoothness = 0.25,
        HeadOnly = true,
        Prediction = 0.16,
    },
    SilentAim = {
        Enabled = false,
        Key = Enum.UserInputType.MouseButton1,
        TeamCheck = true,
        WallCheck = false,
        FOV = 200,
        HeadOnly = true,
        Prediction = 0.16,
        HitChance = 100,
    },
    AutoRotate = {
        Enabled = false,
        Key = Enum.KeyCode.E,      -- desktop bind
        Range = 60,                -- studs — target acquisition radius
        HeadOnly = true,
        Smoothness = 0.15,
        IncludeBehind = true,      -- 360° vs front-only
    },
    Bhop = {
        Enabled = false,
        Key = Enum.KeyCode.Space,
        AutoStrafe = true,
    },
    ESP = {
        Enabled = true,
        Box = true,
        Name = true,
        Health = true,
        Distance = true,
        Skeleton = true,
        TeamCheck = true,
        MaxDistance = 1000,
        Color = Color3.fromRGB(0, 255, 170),
        TeamColor = Color3.fromRGB(255, 80, 80),
    },
}

-- ============ HELPERS ============
local function isAlive(plr)
    local char = plr.Character
    if not char then return false end
    local hum = char:FindFirstChildOfClass("Humanoid")
    return hum and hum.Health > 0
end

local function getTargetPart(plr, headOnly)
    local char = plr.Character
    if not char then return nil end
    if headOnly then
        return char:FindFirstChild("Head")
    end
    return char:FindFirstChild("UpperTorso")
        or char:FindFirstChild("Torso")
        or char:FindFirstChild("HumanoidRootPart")
end

local function isVisible(part, ignoreList)
    if not part then return false end
    local origin = Camera.CFrame.Position
    local dir = part.Position - origin
    local params = RaycastParams.new()
    params.FilterType = Enum.RaycastFilterType.Exclude
    params.FilterDescendantsInstances = ignoreList or { LocalPlayer.Character, Camera }
    params.IgnoreWater = true
    local result = Workspace:Raycast(origin, dir, params)
    return result == nil or result.Instance:IsDescendantOf(part.Parent)
end

-- ============ FOV CIRCLE ============
local fovGui = Instance.new("ScreenGui")
fovGui.Name = "PlayerList"
fovGui.ResetOnSpawn = false
fovGui.IgnoreGuiInset = true
secureParent(fovGui)

local fovCircle = Instance.new("Frame", fovGui)
fovCircle.BackgroundTransparency = 1
fovCircle.Size = UDim2.new(0, 0, 0, 0)
fovCircle.AnchorPoint = Vector2.new(0.5, 0.5)
fovCircle.Position = UDim2.new(0.5, 0, 0.5, 0)
fovCircle.Visible = false

local fovStroke = Instance.new("UIStroke", fovCircle)
fovStroke.Thickness = 1
fovStroke.Color = Color3.fromRGB(0, 255, 170)
fovStroke.Transparency = 0.4

local fovCorner = Instance.new("UICorner", fovCircle)
fovCorner.CornerRadius = UDim.new(1, 0)

RunService.RenderStepped:Connect(function()
    local active = nil
    if Config.Aimbot.Enabled then active = Config.Aimbot.FOV end
    if Config.SilentAim.Enabled then active = math.max(active or 0, Config.SilentAim.FOV) end
    if Config.AutoRotate.Enabled then active = math.max(active or 0, 400) end

    if active and active > 0 then
        fovCircle.Visible = true
        fovCircle.Size = UDim2.new(0, active * 2, 0, active * 2)
    else
        fovCircle.Visible = false
    end
end)

-- ============ TARGET SELECTION ============
local function getClosestInFOV(fov, teamCheck, headOnly, wallCheck)
    local closest, shortest = nil, fov
    local center = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)

    for _, plr in ipairs(Players:GetPlayers()) do
        if plr == LocalPlayer then continue end
        if not isAlive(plr) then continue end
        if teamCheck and plr.Team == LocalPlayer.Team then continue end

        local part = getTargetPart(plr, headOnly)
        if not part then continue end

        local screenPos, onScreen = Camera:WorldToViewportPoint(part.Position)
        if not onScreen then continue end

        local dist = (Vector2.new(screenPos.X, screenPos.Y) - center).Magnitude
        if dist < shortest then
            if wallCheck and not isVisible(part, { LocalPlayer.Character, Camera }) then
                continue
            end
            shortest = dist
            closest = plr
        end
    end
    return closest
end

-- Closest by 3D distance — used by AutoRotate (ignores FOV, can hit behind)
local function getClosestInRange(range, teamCheck, headOnly, includeBehind)
    local closest, shortest = nil, range
    local myRoot = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    if not myRoot then return nil end

    local camLook = Camera.CFrame.LookVector

    for _, plr in ipairs(Players:GetPlayers()) do
        if plr == LocalPlayer then continue end
        if not isAlive(plr) then continue end
        if teamCheck and plr.Team == LocalPlayer.Team then continue end

        local part = getTargetPart(plr, headOnly)
        if not part then continue end

        local delta = part.Position - myRoot.Position
        local dist = delta.Magnitude
        if dist > shortest then continue end

        if not includeBehind then
            local toTarget = delta.Unit
            if camLook:Dot(toTarget) < 0.3 then continue end
        end

        shortest = dist
        closest = plr
    end
    return closest
end

-- ============ AIMBOT ============
local aimbotHeld = false
UserInputService.InputBegan:Connect(function(input, gp)
    if gp then return end
    if input.UserInputType == Config.Aimbot.Key or input.KeyCode == Enum.KeyCode.ButtonR2 then
        aimbotHeld = true
    end
end)
UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Config.Aimbot.Key or input.KeyCode == Enum.KeyCode.ButtonR2 then
        aimbotHeld = false
    end
end)

RunService.RenderStepped:Connect(function()
    if not Config.Aimbot.Enabled or not aimbotHeld then return end
    local target = getClosestInFOV(
        Config.Aimbot.FOV, Config.Aimbot.TeamCheck,
        Config.Aimbot.HeadOnly, Config.Aimbot.WallCheck
    )
    if not target then return end

    local part = getTargetPart(target, Config.Aimbot.HeadOnly)
    local root = target.Character and target.Character:FindFirstChild("HumanoidRootPart")
    if not part then return end

    local aimPos = part.Position
    if root and Config.Aimbot.Prediction > 0 then
        aimPos = part.Position + root.AssemblyLinearVelocity * Config.Aimbot.Prediction
    end

    local desired = CFrame.new(Camera.CFrame.Position, aimPos)
    Camera.CFrame = Camera.CFrame:Lerp(desired, 1 - Config.Aimbot.Smoothness)
end)

-- ============ AUTO ROTATE (360° kill assist) ============
local autoRotateActive = false
UserInputService.InputBegan:Connect(function(input, gp)
    if gp then return end
    if input.KeyCode == Config.AutoRotate.Key or input.KeyCode == Enum.KeyCode.ButtonL2 then
        autoRotateActive = true
    end
end)
UserInputService.InputEnded:Connect(function(input)
    if input.KeyCode == Config.AutoRotate.Key or input.KeyCode == Enum.KeyCode.ButtonL2 then
        autoRotateActive = false
    end
end)

RunService.RenderStepped:Connect(function()
    if not Config.AutoRotate.Enabled or not autoRotateActive then return end
    local target = getClosestInRange(
        Config.AutoRotate.Range, Config.Aimbot.TeamCheck,
        Config.AutoRotate.HeadOnly, Config.AutoRotate.IncludeBehind
    )
    if not target then return end

    local part = getTargetPart(target, Config.AutoRotate.HeadOnly)
    local root = target.Character and target.Character:FindFirstChild("HumanoidRootPart")
    if not part then return end

    local aimPos = part.Position
    if root then
        aimPos = part.Position + root.AssemblyLinearVelocity * 0.12
    end

    local desired = CFrame.new(Camera.CFrame.Position, aimPos)
    Camera.CFrame = Camera.CFrame:Lerp(desired, 1 - Config.AutoRotate.Smoothness)
end)

-- ============ SILENT AIM (recursion-guarded) ============
local hookBusy = false    -- reentrancy guard — this is the crash fix

local mt = getrawmetatable(game)
local oldNamecall = mt.__namecall
setreadonly(mt, false)

mt.__namecall = newcclosure(function(self, ...)
    local method = getnamecallmethod()

    -- Bail immediately if: hook disabled, we're already inside it,
    -- the call originated from our own thread, or the busy flag is up.
    if hookBusy
       or not Config.SilentAim.Enabled
       or checkcaller()
       or typeof(method) ~= "string" then
        return oldNamecall(self, ...)
    end

    if method ~= "FindPartOnRay"
       and method ~= "FindPartOnRayWithIgnoreList"
       and method ~= "FindPartOnRayWithWhitelist"
       and method ~= "Raycast" then
        return oldNamecall(self, ...)
    end

    -- Hit chance gate — random early-exit
    if Config.SilentAim.HitChance < 100 and math.random(1, 100) > Config.SilentAim.HitChance then
        return oldNamecall(self, ...)
    end

    -- Lock the guard BEFORE doing anything that could re-enter
    hookBusy = true

    local ok, ret = pcall(function()
        local target = getClosestInFOV(
            Config.SilentAim.FOV, Config.SilentAim.TeamCheck,
            Config.SilentAim.HeadOnly, Config.SilentAim.WallCheck
        )
        if not target then return oldNamecall(self, ...) end

        local part = getTargetPart(target, Config.SilentAim.HeadOnly)
        local root = target.Character and target.Character:FindFirstChild("HumanoidRootPart")
        if not part then return oldNamecall(self, ...) end

        local aimPos = part.Position
        if root and Config.SilentAim.Prediction > 0 then
            aimPos = part.Position + root.AssemblyLinearVelocity * Config.SilentAim.Prediction
        end

        local args = { ... }

        if method == "FindPartOnRay"
           or method == "FindPartOnRayWithIgnoreList"
           or method == "FindPartOnRayWithWhitelist" then
            local oldRay = args[1]
            if typeof(oldRay) == "Ray" then
                args[1] = Ray.new(oldRay.Origin, aimPos - oldRay.Origin)
            end
        elseif method == "Raycast" and typeof(self) == "Instance" and self:IsA("Workspace") then
            local origin = args[1]
            local dir = args[2]
            if typeof(origin) == "Vector3" then
                local newDir = aimPos - origin
                if typeof(dir) == "Vector3" and dir.Magnitude > 0 then
                    newDir = newDir.Unit * dir.Magnitude
                end
                args[2] = newDir
            end
        end

        return oldNamecall(self, unpack(args))
    end)

    hookBusy = false
    if ok then return ret end
    return oldNamecall(self, ...)
end)

setreadonly(mt, true)

-- ============ BUNNY HOP ============
local bhopHeld = false
UserInputService.InputBegan:Connect(function(input, gp)
    if gp then return end
    if input.KeyCode == Config.Bhop.Key or input.KeyCode == Enum.KeyCode.ButtonA then
        bhopHeld = true
    end
end)
UserInputService.InputEnded:Connect(function(input)
    if input.KeyCode == Config.Bhop.Key or input.KeyCode == Enum.KeyCode.ButtonA then
        bhopHeld = false
    end
end)

RunService.Heartbeat:Connect(function()
    if not Config.Bhop.Enabled or not bhopHeld then return end
    local char = LocalPlayer.Character
    if not char then return end
    local hum = char:FindFirstChildOfClass("Humanoid")
    if not hum then return end
    if hum.FloorMaterial ~= Enum.Material.Air then
        hum.Jump = true
    end
    if Config.Bhop.AutoStrafe then
        local root = char:FindFirstChild("HumanoidRootPart")
        if root then
            root.CFrame = root.CFrame * CFrame.Angles(0, math.rad(2), 0)
        end
    end
end)

-- ============ ESP ============
local espCache = {}

local function makeGui(plr)
    if espCache[plr] then return end

    local billboard = Instance.new("BillboardGui")
    billboard.Name = "PlayerTag"
    billboard.Size = UDim2.new(0, 100, 0, 60)
    billboard.AlwaysOnTop = true
    billboard.ResetOnSpawn = false
    billboard.StudsOffset = Vector3.new(0, 3, 0)

    local container = Instance.new("Frame", billboard)
    container.BackgroundTransparency = 1
    container.Size = UDim2.new(1, 0, 1, 0)

    local box = Instance.new("Frame", container)
    box.BackgroundTransparency = 1
    box.Size = UDim2.new(1, 0, 1, 0)
    local boxStroke = Instance.new("UIStroke", box)
    boxStroke.Thickness = 1.5
    boxStroke.Color = Config.ESP.Color

    local nameLabel = Instance.new("TextLabel", container)
    nameLabel.BackgroundTransparency = 1
    nameLabel.Size = UDim2.new(1, 0, 0, 14)
    nameLabel.Position = UDim2.new(0, 0, 0, -16)
    nameLabel.TextColor3 = Config.ESP.Color
    nameLabel.TextStrokeTransparency = 0
    nameLabel.TextSize = 12
    nameLabel.Font = Enum.Font.GothamBold
    nameLabel.Text = plr.Name

    local distLabel = Instance.new("TextLabel", container)
    distLabel.BackgroundTransparency = 1
    distLabel.Size = UDim2.new(1, 0, 0, 14)
    distLabel.Position = UDim2.new(0, 0, 1, 2)
    distLabel.TextColor3 = Config.ESP.Color
    distLabel.TextStrokeTransparency = 0
    distLabel.TextSize = 11
    distLabel.Font = Enum.Font.Gotham

    local healthBg = Instance.new("Frame", container)
    healthBg.BackgroundColor3 = Color3.new(0, 0, 0)
    healthBg.BorderSizePixel = 0
    healthBg.Size = UDim2.new(0, 3, 1, 0)
    healthBg.Position = UDim2.new(0, -6, 0, 0)
    local healthFill = Instance.new("Frame", healthBg)
    healthFill.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
    healthFill.BorderSizePixel = 0
    healthFill.Size = UDim2.new(1, 0, 1, 0)

    local skeletonFolder = Instance.new("Folder", container)
    skeletonFolder.Name = "Skeleton"
    local lines = {}
    for i = 1, 20 do
        local line = Instance.new("Frame", skeletonFolder)
        line.BackgroundColor3 = Config.ESP.Color
        line.BorderSizePixel = 0
        line.AnchorPoint = Vector2.new(0.5, 0.5)
        line.ZIndex = 2
        line.Visible = false
        lines[i] = line
    end

    espCache[plr] = {
        billboard = billboard,
        box = box, boxStroke = boxStroke,
        nameLabel = nameLabel,
        distLabel = distLabel,
        healthFill = healthFill,
        healthBg = healthBg,
        lines = lines,
    }

    billboard.Parent = fovGui
end

local function removeGui(plr)
    local data = espCache[plr]
    if not data then return end
    if data.billboard then data.billboard:Destroy() end
    espCache[plr] = nil
end

Players.PlayerAdded:Connect(function(plr) if plr ~= LocalPlayer then makeGui(plr) end end)
Players.PlayerRemoving:Connect(removeGui)
for _, plr in ipairs(Players:GetPlayers()) do
    if plr ~= LocalPlayer then makeGui(plr) end
end

local SKELETON = {
    {"Head","UpperTorso"},{"UpperTorso","LowerTorso"},
    {"UpperTorso","LeftUpperArm"},{"LeftUpperArm","LeftLowerArm"},{"LeftLowerArm","LeftHand"},
    {"UpperTorso","RightUpperArm"},{"RightUpperArm","RightLowerArm"},{"RightLowerArm","RightHand"},
    {"LowerTorso","LeftUpperLeg"},{"LeftUpperLeg","LeftLowerLeg"},{"LeftLowerLeg","LeftFoot"},
    {"LowerTorso","RightUpperLeg"},{"RightUpperLeg","RightLowerLeg"},{"RightLowerLeg","RightFoot"},
}

RunService.RenderStepped:Connect(function()
    for plr, data in pairs(espCache) do
        local char = plr.Character
        local hum  = char and char:FindFirstChildOfClass("Humanoid")
        local root = char and char:FindFirstChild("HumanoidRootPart")

        if not (Config.ESP.Enabled and char and hum and hum.Health > 0 and root) then
            data.billboard.Enabled = false
            continue
        end

        local dist = (Camera.CFrame.Position - root.Position).Magnitude
        if dist > Config.ESP.MaxDistance then
            data.billboard.Enabled = false
            continue
        end

        data.billboard.Adornee = root
        data.billboard.Enabled = true

        local isTeam = Config.ESP.TeamCheck and plr.Team == LocalPlayer.Team
        local color = isTeam and Config.ESP.TeamColor or Config.ESP.Color
        data.boxStroke.Color = color
        data.nameLabel.TextColor3 = color
        data.distLabel.TextColor3 = color

        data.nameLabel.Text = plr.Name
        data.nameLabel.Visible = Config.ESP.Name
        data.box.Visible = Config.ESP.Box
        data.boxStroke.Enabled = Config.ESP.Box

        if Config.ESP.Distance then
            data.distLabel.Text = string.format("[%d]", math.floor(dist))
            data.distLabel.Visible = true
        else
            data.distLabel.Visible = false
        end

        if Config.ESP.Health then
            local hp = math.clamp(hum.Health / hum.MaxHealth, 0, 1)
            data.healthBg.Visible = true
            data.healthFill.Size = UDim2.new(1, 0, hp, 0)
            data.healthFill.Position = UDim2.new(0, 0, 1 - hp, 0)
            data.healthFill.BackgroundColor3 = Color3.fromRGB(255 * (1 - hp), 255 * hp, 0)
        else
            data.healthBg.Visible = false
        end

        if Config.ESP.Skeleton then
            for i, pair in ipairs(SKELETON) do
                local a = char:FindFirstChild(pair[1])
                local b = char:FindFirstChild(pair[2])
                local line = data.lines[i]
                if a and b then
                    local aPos, aOn = Camera:WorldToViewportPoint(a.Position)
                    local bPos, bOn = Camera:WorldToViewportPoint(b.Position)
                    if aOn and bOn then
                        local delta = Vector2.new(bPos.X - aPos.X, bPos.Y - aPos.Y)
                        local length = delta.Magnitude
                        local angle = math.deg(math.atan2(delta.Y, delta.X))
                        line.Size = UDim2.new(0, length, 0, 1)
                        line.Position = UDim2.new(0, aPos.X, 0, aPos.Y)
                        line.Rotation = angle
                        line.Visible = true
                        line.BackgroundColor3 = color
                    else
                        line.Visible = false
                    end
                else
                    line.Visible = false
                end
            end
        else
            for _, line in pairs(data.lines) do line.Visible = false end
        end
    end
end)

-- ============ UI ============
local gui = Instance.new("ScreenGui")
gui.Name = "Settings"
gui.ResetOnSpawn = false
gui.IgnoreGuiInset = true
secureParent(gui)

local frame = Instance.new("Frame", gui)
frame.Size = UDim2.new(0, 280, 0, 420)
frame.Position = UDim2.new(0, 20, 0.5, -210)
frame.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
frame.BackgroundTransparency = 0.05
frame.BorderSizePixel = 0
frame.Active = true
frame.Draggable = true
frame.ClipsDescendants = true

local corner = Instance.new("UICorner", frame)
corner.CornerRadius = UDim.new(0, 8)

local title = Instance.new("TextButton", frame)
title.Size = UDim2.new(1, 0, 0, 30)
title.BackgroundColor3 = Color3.fromRGB(25, 25, 32)
title.Text = "Lunar | v3"
title.TextColor3 = Color3.fromRGB(0, 255, 170)
title.Font = Enum.Font.GothamBold
title.TextSize = 14
title.BorderSizePixel = 0
title.AutoButtonColor = false

local titleCorner = Instance.new("UICorner", title)
titleCorner.CornerRadius = UDim.new(0, 8)

local scroll = Instance.new("ScrollingFrame", frame)
scroll.Size = UDim2.new(1, -10, 1, -40)
scroll.Position = UDim2.new(0, 5, 0, 35)
scroll.BackgroundTransparency = 1
scroll.BorderSizePixel = 0
scroll.ScrollBarThickness = 3
scroll.CanvasSize = UDim2.new(0, 0, 0, 600)

-- Minimize toggle — same button expands
local minimized = false
local function setMinimized(v)
    minimized = v
    if v then
        frame.Size = UDim2.new(0, 280, 0, 30)
        scroll.Visible = false
        title.Text = "Lunar | v3  [+]"
    else
        frame.Size = UDim2.new(0, 280, 0, 420)
        scroll.Visible = true
        title.Text = "Lunar | v3  [-]"
    end
end

title.MouseButton1Click:Connect(function()
    setMinimized(not minimized)
end)

local y = 0
local function addToggle(name, getter, setter)
    local btn = Instance.new("TextButton", scroll)
    btn.Size = UDim2.new(1, 0, 0, 26)
    btn.Position = UDim2.new(0, 0, 0, y)
    btn.BackgroundColor3 = Color3.fromRGB(25, 25, 32)
    btn.TextColor3 = getter() and Color3.fromRGB(0, 255, 170) or Color3.fromRGB(180, 180, 180)
    btn.Text = (getter() and "[+] " or "[-] ") .. name
    btn.Font = Enum.Font.Gotham
    btn.TextSize = 12
    btn.BorderSizePixel = 0
    btn.MouseButton1Click:Connect(function()
        setter(not getter())
        btn.Text = (getter() and "[+] " or "[-] ") .. name
        btn.TextColor3 = getter() and Color3.fromRGB(0, 255, 170) or Color3.fromRGB(180, 180, 180)
    end)
    y = y + 28
end

local function addSlider(name, getter, setter, min, max, step)
    local container = Instance.new("Frame", scroll)
    container.Size = UDim2.new(1, 0, 0, 32)
    container.Position = UDim2.new(0, 0, 0, y)
    container.BackgroundColor3 = Color3.fromRGB(25, 25, 32)
    container.BorderSizePixel = 0

    local label = Instance.new("TextLabel", container)
    label.Size = UDim2.new(0.6, 0, 0, 32)
    label.Position = UDim2.new(0, 6, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = name .. ": " .. tostring(getter())
    label.TextColor3 = Color3.fromRGB(180, 180, 255)
    label.Font = Enum.Font.Gotham
    label.TextSize = 11
    label.TextXAlignment = Enum.TextXAlignment.Left

    local dec = Instance.new("TextButton", container)
    dec.Size = UDim2.new(0, 24, 0, 20)
    dec.Position = UDim2.new(1, -56, 0.5, -10)
    dec.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
    dec.TextColor3 = Color3.fromRGB(200, 200, 200)
    dec.Text = "-"
    dec.Font = Enum.Font.GothamBold
    dec.TextSize = 12
    dec.BorderSizePixel = 0

    local inc = Instance.new("TextButton", container)
    inc.Size = UDim2.new(0, 24, 0, 20)
    inc.Position = UDim2.new(1, -28, 0.5, -10)
    inc.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
    inc.TextColor3 = Color3.fromRGB(200, 200, 200)
    inc.Text = "+"
    inc.Font = Enum.Font.GothamBold
    inc.TextSize = 12
    inc.BorderSizePixel = 0

    dec.MouseButton1Click:Connect(function()
        local v = math.max(min, getter() - step)
        setter(v)
        label.Text = name .. ": " .. tostring(v)
    end)
    inc.MouseButton1Click:Connect(function()
        local v = math.min(max, getter() + step)
        setter(v)
        label.Text = name .. ": " .. tostring(v)
    end)

    y = y + 34
end

addToggle("Aimbot", function() return Config.Aimbot.Enabled end, function(v) Config.Aimbot.Enabled = v end)
addToggle("  Head Only", function() return Config.Aimbot.HeadOnly end, function(v)
    Config.Aimbot.HeadOnly = v
    Config.SilentAim.HeadOnly = v
end)
addSlider("  Aimbot FOV", function() return Config.Aimbot.FOV end, function(v) Config.Aimbot.FOV = v end, 20, 500, 10)
addSlider("  Aimbot Smooth", function() return math.floor(Config.Aimbot.Smoothness * 100) end, function(v) Config.Aimbot.Smoothness = v / 100 end, 5, 95, 5)

addToggle("Silent Aim", function() return Config.SilentAim.Enabled end, function(v) Config.SilentAim.Enabled = v end)
addSlider("  Silent FOV", function() return Config.SilentAim.FOV end, function(v) Config.SilentAim.FOV = v end, 20, 500, 10)
addSlider("  Hit Chance %", function() return Config.SilentAim.HitChance end, function(v) Config.SilentAim.HitChance = v end, 10, 100, 5)

addToggle("Auto Rotate (360)", function() return Config.AutoRotate.Enabled end, function(v) Config.AutoRotate.Enabled = v end)
addSlider("  Rotate Range", function() return Config.AutoRotate.Range end, function(v) Config.AutoRotate.Range = v end, 10, 200, 5)
addToggle("  Include Behind", function() return Config.AutoRotate.IncludeBehind end, function(v) Config.AutoRotate.IncludeBehind = v end)

addToggle("Bunny Hop", function() return Config.Bhop.Enabled end, function(v) Config.Bhop.Enabled = v end)
addToggle("  Auto Strafe", function() return Config.Bhop.AutoStrafe end, function(v) Config.Bhop.AutoStrafe = v end)

addToggle("ESP Master", function() return Config.ESP.Enabled end, function(v) Config.ESP.Enabled = v end)
addToggle("  Box", function() return Config.ESP.Box end, function(v) Config.ESP.Box = v end)
addToggle("  Name", function() return Config.ESP.Name end, function(v) Config.ESP.Name = v end)
addToggle("  Health", function() return Config.ESP.Health end, function(v) Config.ESP.Health = v end)
addToggle("  Distance", function() return Config.ESP.Distance end, function(v) Config.ESP.Distance = v end)
addToggle("  Skeleton", function() return Config.ESP.Skeleton end, function(v) Config.ESP.Skeleton = v end)
addToggle("Team Check", function() return Config.ESP.TeamCheck end, function(v)
    Config.ESP.TeamCheck = v
    Config.Aimbot.TeamCheck = v
    Config.SilentAim.TeamCheck = v
end)
addToggle("Wall Check", function() return Config.Aimbot.WallCheck end, function(v)
    Config.Aimbot.WallCheck = v
    Config.SilentAim.WallCheck = v
end)

scroll.CanvasSize = UDim2.new(0, 0, 0, y + 10)

-- Desktop keybind
UserInputService.InputBegan:Connect(function(input, gp)
    if gp then return end
    if input.KeyCode == Enum.KeyCode.RightShift then
        setMinimized(not minimized)
    end
end)

-- Mobile floating toggle
local openBtn = Instance.new("TextButton", gui)
openBtn.Size = UDim2.new(0, 50, 0, 50)
openBtn.Position = UDim2.new(0, 20, 0.5, 220)
openBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 32)
openBtn.TextColor3 = Color3.fromRGB(0, 255, 170)
openBtn.Text = "L"
openBtn.Font = Enum.Font.GothamBold
openBtn.TextSize = 20
openBtn.BorderSizePixel = 0
openBtn.Visible = UserInputService.TouchEnabled
openBtn.MouseButton1Click:Connect(function()
    frame.Visible = not frame.Visible
end)

local openCorner = Instance.new("UICorner", openBtn)
openCorner.CornerRadius = UDim.new(1, 0)

print("[Lunar] v3 loaded. RightShift or L-button to toggle. Title bar to minimize.")